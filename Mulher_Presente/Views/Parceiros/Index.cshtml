@model IEnumerable<Mulher_Presente.Models.Parceiros>

@{
    ViewData["Title"] = "Parceria";
}

<!DOCTYPE html>

<html>
<head>
    <link href="~/css/style.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <style>
        .cor-fundo{
             background: rgba(0, 0, 0, 0.7);
        }
    </style>
</head>

<body>
    <div class="text-center">
        <h5 class="text-white h3 m-3">
            Através do seu cadastro você vai poderá auxiliar mulheres em todo o Brasil
            que foram vítimas de algum tipo de violência.
        </h5>


        <p class="  "><a asp-action="Create" class="btn btn-outline-warning h3" >Seja nosso parceiro</a></p>
    </div>
    <hr class="bg-white"/>

<div class="d-flex flex-wrap">
@foreach (var item in Model) {
<div class="card m-3 cor-fundo  " style="width: 18rem;">
  <div class="card-body text-white ">
    <h5 class="text-white ">@Html.DisplayFor(modelItem => item.Especilidade)</h5>
    <h6 class="card-subtitle mb-2 text-white"> @Html.DisplayFor(modelItem => item.Nome)</h6>
    <p class=" text-white">📞 @Html.DisplayFor(modelItem => item.Telefone)</p>
    <p class=" text-white">@Html.DisplayFor(modelItem => item.email)</p>
        <a asp-action="Edit" asp-route-id="@item.id_parceiro">Editar</a> |
        <a asp-action="Details" asp-route-id="@item.id_parceiro">Mostrar</a> |
        <a asp-action="Delete" asp-route-id="@item.id_parceiro">Excluir</a>
  </div>
</div>
}
</div>



</body>
</html>
