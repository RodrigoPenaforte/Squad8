@model IEnumerable<Mulher_Presente.Models.ParceirosUser>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefone_parceiro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email_parceiro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Especialidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefone_parceiro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email_parceiro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Especialidade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_parceiro">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_parceiro">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_parceiro">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
